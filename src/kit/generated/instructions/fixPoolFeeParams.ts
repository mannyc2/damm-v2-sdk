/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CP_AMM_PROGRAM_ADDRESS } from "../programs";
import {
  getBaseFeeParametersDecoder,
  getBaseFeeParametersEncoder,
  type BaseFeeParameters,
  type BaseFeeParametersArgs,
} from "../types";

export const FIX_POOL_FEE_PARAMS_DISCRIMINATOR = new Uint8Array([
  132, 98, 81, 196, 44, 58, 120, 193,
]);

export function getFixPoolFeeParamsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FIX_POOL_FEE_PARAMS_DISCRIMINATOR,
  );
}

export type FixPoolFeeParamsInstruction<
  TProgram extends string = typeof CP_AMM_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountOperator extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountOperator extends string
        ? ReadonlyAccount<TAccountOperator>
        : TAccountOperator,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type FixPoolFeeParamsInstructionData = {
  discriminator: ReadonlyUint8Array;
  params: BaseFeeParameters;
};

export type FixPoolFeeParamsInstructionDataArgs = {
  params: BaseFeeParametersArgs;
};

export function getFixPoolFeeParamsInstructionDataEncoder(): FixedSizeEncoder<FixPoolFeeParamsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["params", getBaseFeeParametersEncoder()],
    ]),
    (value) => ({ ...value, discriminator: FIX_POOL_FEE_PARAMS_DISCRIMINATOR }),
  );
}

export function getFixPoolFeeParamsInstructionDataDecoder(): FixedSizeDecoder<FixPoolFeeParamsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["params", getBaseFeeParametersDecoder()],
  ]);
}

export function getFixPoolFeeParamsInstructionDataCodec(): FixedSizeCodec<
  FixPoolFeeParamsInstructionDataArgs,
  FixPoolFeeParamsInstructionData
> {
  return combineCodec(
    getFixPoolFeeParamsInstructionDataEncoder(),
    getFixPoolFeeParamsInstructionDataDecoder(),
  );
}

export type FixPoolFeeParamsInput<
  TAccountPool extends string = string,
  TAccountOperator extends string = string,
  TAccountSigner extends string = string,
> = {
  pool: Address<TAccountPool>;
  operator: Address<TAccountOperator>;
  signer: TransactionSigner<TAccountSigner>;
  params: FixPoolFeeParamsInstructionDataArgs["params"];
};

export function getFixPoolFeeParamsInstruction<
  TAccountPool extends string,
  TAccountOperator extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof CP_AMM_PROGRAM_ADDRESS,
>(
  input: FixPoolFeeParamsInput<TAccountPool, TAccountOperator, TAccountSigner>,
  config?: { programAddress?: TProgramAddress },
): FixPoolFeeParamsInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountOperator,
  TAccountSigner
> {
  // Program address.
  const programAddress = config?.programAddress ?? CP_AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    operator: { value: input.operator ?? null, isWritable: false },
    signer: { value: input.signer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("operator", accounts.operator),
      getAccountMeta("signer", accounts.signer),
    ],
    data: getFixPoolFeeParamsInstructionDataEncoder().encode(
      args as FixPoolFeeParamsInstructionDataArgs,
    ),
    programAddress,
  } as FixPoolFeeParamsInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountOperator,
    TAccountSigner
  >);
}

export type ParsedFixPoolFeeParamsInstruction<
  TProgram extends string = typeof CP_AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    operator: TAccountMetas[1];
    signer: TAccountMetas[2];
  };
  data: FixPoolFeeParamsInstructionData;
};

export function parseFixPoolFeeParamsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFixPoolFeeParamsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      operator: getNextAccount(),
      signer: getNextAccount(),
    },
    data: getFixPoolFeeParamsInstructionDataDecoder().decode(instruction.data),
  };
}
