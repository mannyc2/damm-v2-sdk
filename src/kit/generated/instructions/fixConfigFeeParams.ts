/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CP_AMM_PROGRAM_ADDRESS } from "../programs";
import {
  getBaseFeeParametersDecoder,
  getBaseFeeParametersEncoder,
  type BaseFeeParameters,
  type BaseFeeParametersArgs,
} from "../types";

export const FIX_CONFIG_FEE_PARAMS_DISCRIMINATOR = new Uint8Array([
  38, 30, 216, 81, 250, 177, 243, 254,
]);

export function getFixConfigFeeParamsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FIX_CONFIG_FEE_PARAMS_DISCRIMINATOR,
  );
}

export type FixConfigFeeParamsInstruction<
  TProgram extends string = typeof CP_AMM_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountOperator extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountOperator extends string
        ? ReadonlyAccount<TAccountOperator>
        : TAccountOperator,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type FixConfigFeeParamsInstructionData = {
  discriminator: ReadonlyUint8Array;
  params: BaseFeeParameters;
};

export type FixConfigFeeParamsInstructionDataArgs = {
  params: BaseFeeParametersArgs;
};

export function getFixConfigFeeParamsInstructionDataEncoder(): FixedSizeEncoder<FixConfigFeeParamsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["params", getBaseFeeParametersEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: FIX_CONFIG_FEE_PARAMS_DISCRIMINATOR,
    }),
  );
}

export function getFixConfigFeeParamsInstructionDataDecoder(): FixedSizeDecoder<FixConfigFeeParamsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["params", getBaseFeeParametersDecoder()],
  ]);
}

export function getFixConfigFeeParamsInstructionDataCodec(): FixedSizeCodec<
  FixConfigFeeParamsInstructionDataArgs,
  FixConfigFeeParamsInstructionData
> {
  return combineCodec(
    getFixConfigFeeParamsInstructionDataEncoder(),
    getFixConfigFeeParamsInstructionDataDecoder(),
  );
}

export type FixConfigFeeParamsInput<
  TAccountConfig extends string = string,
  TAccountOperator extends string = string,
  TAccountSigner extends string = string,
> = {
  config: Address<TAccountConfig>;
  operator: Address<TAccountOperator>;
  signer: TransactionSigner<TAccountSigner>;
  params: FixConfigFeeParamsInstructionDataArgs["params"];
};

export function getFixConfigFeeParamsInstruction<
  TAccountConfig extends string,
  TAccountOperator extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof CP_AMM_PROGRAM_ADDRESS,
>(
  input: FixConfigFeeParamsInput<
    TAccountConfig,
    TAccountOperator,
    TAccountSigner
  >,
  config?: { programAddress?: TProgramAddress },
): FixConfigFeeParamsInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountOperator,
  TAccountSigner
> {
  // Program address.
  const programAddress = config?.programAddress ?? CP_AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    operator: { value: input.operator ?? null, isWritable: false },
    signer: { value: input.signer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("operator", accounts.operator),
      getAccountMeta("signer", accounts.signer),
    ],
    data: getFixConfigFeeParamsInstructionDataEncoder().encode(
      args as FixConfigFeeParamsInstructionDataArgs,
    ),
    programAddress,
  } as FixConfigFeeParamsInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOperator,
    TAccountSigner
  >);
}

export type ParsedFixConfigFeeParamsInstruction<
  TProgram extends string = typeof CP_AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    operator: TAccountMetas[1];
    signer: TAccountMetas[2];
  };
  data: FixConfigFeeParamsInstructionData;
};

export function parseFixConfigFeeParamsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFixConfigFeeParamsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      operator: getNextAccount(),
      signer: getNextAccount(),
    },
    data: getFixConfigFeeParamsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
