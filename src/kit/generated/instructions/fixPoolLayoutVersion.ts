/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CP_AMM_PROGRAM_ADDRESS } from "../programs";

export const FIX_POOL_LAYOUT_VERSION_DISCRIMINATOR = new Uint8Array([
  166, 158, 69, 35, 81, 167, 200, 215,
]);

export function getFixPoolLayoutVersionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FIX_POOL_LAYOUT_VERSION_DISCRIMINATOR,
  );
}

export type FixPoolLayoutVersionInstruction<
  TProgram extends string = typeof CP_AMM_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountOperator extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountOperator extends string
        ? ReadonlyAccount<TAccountOperator>
        : TAccountOperator,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type FixPoolLayoutVersionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FixPoolLayoutVersionInstructionDataArgs = {};

export function getFixPoolLayoutVersionInstructionDataEncoder(): FixedSizeEncoder<FixPoolLayoutVersionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: FIX_POOL_LAYOUT_VERSION_DISCRIMINATOR,
    }),
  );
}

export function getFixPoolLayoutVersionInstructionDataDecoder(): FixedSizeDecoder<FixPoolLayoutVersionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFixPoolLayoutVersionInstructionDataCodec(): FixedSizeCodec<
  FixPoolLayoutVersionInstructionDataArgs,
  FixPoolLayoutVersionInstructionData
> {
  return combineCodec(
    getFixPoolLayoutVersionInstructionDataEncoder(),
    getFixPoolLayoutVersionInstructionDataDecoder(),
  );
}

export type FixPoolLayoutVersionInput<
  TAccountPool extends string = string,
  TAccountOperator extends string = string,
  TAccountSigner extends string = string,
> = {
  pool: Address<TAccountPool>;
  operator: Address<TAccountOperator>;
  signer: TransactionSigner<TAccountSigner>;
};

export function getFixPoolLayoutVersionInstruction<
  TAccountPool extends string,
  TAccountOperator extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof CP_AMM_PROGRAM_ADDRESS,
>(
  input: FixPoolLayoutVersionInput<
    TAccountPool,
    TAccountOperator,
    TAccountSigner
  >,
  config?: { programAddress?: TProgramAddress },
): FixPoolLayoutVersionInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountOperator,
  TAccountSigner
> {
  // Program address.
  const programAddress = config?.programAddress ?? CP_AMM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    operator: { value: input.operator ?? null, isWritable: false },
    signer: { value: input.signer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("operator", accounts.operator),
      getAccountMeta("signer", accounts.signer),
    ],
    data: getFixPoolLayoutVersionInstructionDataEncoder().encode({}),
    programAddress,
  } as FixPoolLayoutVersionInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountOperator,
    TAccountSigner
  >);
}

export type ParsedFixPoolLayoutVersionInstruction<
  TProgram extends string = typeof CP_AMM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    operator: TAccountMetas[1];
    signer: TAccountMetas[2];
  };
  data: FixPoolLayoutVersionInstructionData;
};

export function parseFixPoolLayoutVersionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFixPoolLayoutVersionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      operator: getNextAccount(),
      signer: getNextAccount(),
    },
    data: getFixPoolLayoutVersionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
