/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * we denote reference_amount = x0, cliff_fee_numerator = c, fee_increment = i
 * if input_amount <= x0, then fee = input_amount * c
 *
 * if input_amount > x0, then input_amount = x0 + (a * x0 + b)
 * if a < max_index
 * then fee = x0 * c + x0 * (c + i) + .... + x0 * (c + i*a) + b * (c + i * (a+1))
 * then fee = x0 * (c + c*a + i*a*(a+1)/2) + b * (c + i * (a+1))
 *
 * if a >= max_index
 * if a = max_index + d, input_amount = x0 + max_index * x0 + (d * x0 + b)
 * then fee = x0 * (c + c*max_index + i*max_index*(max_index+1)/2) + (d * x0 + b) * MAX_FEE
 */
export type BorshFeeRateLimiter = {
  cliffFeeNumerator: bigint;
  feeIncrementBps: number;
  maxLimiterDuration: number;
  maxFeeBps: number;
  referenceAmount: bigint;
  baseFeeMode: number;
};

export type BorshFeeRateLimiterArgs = {
  cliffFeeNumerator: number | bigint;
  feeIncrementBps: number;
  maxLimiterDuration: number;
  maxFeeBps: number;
  referenceAmount: number | bigint;
  baseFeeMode: number;
};

export function getBorshFeeRateLimiterEncoder(): FixedSizeEncoder<BorshFeeRateLimiterArgs> {
  return getStructEncoder([
    ["cliffFeeNumerator", getU64Encoder()],
    ["feeIncrementBps", getU16Encoder()],
    ["maxLimiterDuration", getU32Encoder()],
    ["maxFeeBps", getU32Encoder()],
    ["referenceAmount", getU64Encoder()],
    ["baseFeeMode", getU8Encoder()],
  ]);
}

export function getBorshFeeRateLimiterDecoder(): FixedSizeDecoder<BorshFeeRateLimiter> {
  return getStructDecoder([
    ["cliffFeeNumerator", getU64Decoder()],
    ["feeIncrementBps", getU16Decoder()],
    ["maxLimiterDuration", getU32Decoder()],
    ["maxFeeBps", getU32Decoder()],
    ["referenceAmount", getU64Decoder()],
    ["baseFeeMode", getU8Decoder()],
  ]);
}

export function getBorshFeeRateLimiterCodec(): FixedSizeCodec<
  BorshFeeRateLimiterArgs,
  BorshFeeRateLimiter
> {
  return combineCodec(
    getBorshFeeRateLimiterEncoder(),
    getBorshFeeRateLimiterDecoder(),
  );
}
